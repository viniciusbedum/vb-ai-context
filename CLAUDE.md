@AGENTS.md

# Claude adapter

Treat `AGENTS.md` as the canonical repository instructions. Project skills are exposed to Claude through `.claude/skills/`; each adapter points to the canonical implementation in `.agents/skills/`.

When an adapter is selected, read its canonical `SKILL.md` completely before acting. Do not copy workflow rules into this file.
